<!-- templates/employee/modalDeleteEmployee.html.twig -->
<div class="modal fade" id="modalDeleteEmployee" tabindex="-1" role="dialog" aria-labelledby="modalDeleteEmployeeLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="modalDeleteEmployeeLabel">Excluir Funcionário</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <p>Tem certeza que deseja excluir este funcionário? Esta ação não pode ser desfeita.</p>
        <input type="hidden" id="employeeIdToDelete">
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancelar</button>
        <button type="button" class="btn btn-danger" onclick="deleteEmployee()">Excluir</button>
      </div>
    </div>
  </div>
</div>

<script>
function showDeleteModal(employeeId) {
    $('#employeeIdToDelete').val(employeeId);
    $('#modalDeleteEmployee').modal('show');
}

function deleteEmployee() {
    const employeeId = $('#employeeIdToDelete').val();
    // Adicione aqui a lógica para excluir o funcionário (ex: chamada AJAX para deletar o funcionário)
    alert('Funcionário com ID ' + employeeId + ' excluído!');
    $('#modalDeleteEmployee').modal('hide');
    // Atualize a página ou a tabela de funcionários conforme necessário
}
</script>
